const DeleteLinkedListDuplicate = require('./2.1_LinkedLists');
import {Node} from "./../LinkedLists"

test('adds 1 + 2 to equal 3', () => {
  /*const test1 = new Node(1)
  test1.appendToTail(2);
  test1.appendToTail(2);
  test1.appendToTail(3);
  const result = new Node(1)
  result.appendToTail(2);
  result.appendToTail(3);
  expect(DeleteLinkedListDuplicate(test1)).toBe(result);
  */
  expect(1).toBe(1);
});